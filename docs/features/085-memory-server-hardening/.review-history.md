# Review History — feature 085-memory-server-hardening

## Step 1: Spec-Reviewer Review - Iteration 1 - 2026-04-19T10:45:00Z

**Reviewer:** spec-reviewer (skeptic)
**Decision:** Needs Revision (4 blockers, 6 warnings, 2 suggestions)

**Issues:**
- [blocker] [assumptions] AC-E1 cited `_sanitize_description` as mitigation for description-level `<!--` but source at `_md_insert.py:27-74` does NOT handle HTML comment markers (only `#`, `---`, `===`, triple-backticks). Self-attestation anti-pattern.
  - Suggestion: Reframe AC-E1 — description lands OUTSIDE the `<!-- Promoted: {entry_name} -->` marker, so HTML comment injection doesn't apply at description level.
- [blocker] [testability] SC-9 grep searches for deleted old symbol `_resolve_float_config`; post-FR-4 migration, that name no longer exists → grep returns 0 not 1, vacuous.
  - Suggestion: Grep new helper name; add runtime spy assertion via `unittest.mock.patch`.
- [blocker] [traceability] FR-4 migration missing enumeration of test-file imports. `test_memory_server.py` has 8 references, `test_ranking.py` has 4 references. Also `refresh.py`/`maintenance.py` have int-variant counterpart — scope statement missing.
  - Suggestion: Add Test Migration subsection enumerating every call site; explicit out-of-scope for int-variant.
- [blocker] [testability] SC-5 grep doesn't cover indirect access (getattr, import-from); shell expression returns multiple count lines not aggregated.
  - Suggestion: Use `| wc -l` aggregation + second guard grep over all `.py` for surviving symbol refs.
- [warning] [testability] FR-8 `--exclude-dir=tests` doesn't work — pd tests are inline (e.g., `plugins/pd/hooks/lib/semantic_memory/test_dedup.py`).
  - Suggestion: Use `--exclude='test_*.py'` instead.
- [warning] [testability] SC-11 "proven by deliberate injection in sandbox branch" is manual one-time, not automated CI.
  - Suggestion: Clarify automated guards vs one-time implementation check.
- [warning] [clarity] AC-H5 "called exactly once" lacks test mechanism.
  - Suggestion: Spell out `unittest.mock.patch(..., wraps=...)` spy approach.
- [warning] [testability] FR-7 simple-regex classifier underspecified; no ACs for alternation, char class, quantifier.
  - Suggestion: Commit to `sre_parse`-based classification; add ACs for edge cases.
- [warning] [assumptions] AC-E10 `numpy.bool_` hedge is hand-wave with no evidence.
  - Suggestion: Verify no numpy in pd config; state invariant explicitly.
- [warning] [testability] SC-1 backlog annotation has no verification mechanism.
  - Suggestion: Add explicit grep-based verification.
- [suggestion] [clarity] AC-H3 POSIX-implicit.
- [suggestion] [testability] AC-E5 "undefined behavior" masks practical failure mode.

**Changes Made:**
Complete spec rewrite addressing all 4 blockers + 6 warnings + 2 suggestions:
- AC-E1 reframed: description lands outside comment marker; no sanitization needed for markers at description level.
- SC-9 split into (a) source grep on new helper name + (b) runtime spy.
- FR-4 Test Migration subsection added with 14 line-refs enumerated; int-variant explicitly out of scope with rationale.
- SC-5 split into (a) def-absence grep with `wc -l` + (b) symbol-reference grep across all `.py`.
- SC-11 clarified as automated CI guards + one-time sandbox note.
- AC-H5 test mechanism spelled out with `unittest.mock.patch` wraps/spy.
- FR-7 / AC-H6-H9, E11-E12 committed to `sre_parse`-based classification (`ASSERT`, `GROUPREF`, inline flags = complex).
- AC-E10 documents numpy-bool as static invariant with grep evidence, no new test dep.
- SC-1 specified grep-based verification.
- AC-H3 / AC-E9 explicitly scope to POSIX.
- AC-E5 specifies observable failure mode (diagnostic line possibly landing in `.1` — acceptable for debug-only path).
- Success criteria grew from 14 to 14 items (same count, substantially stronger mechanisms).
- Acceptance criteria grew from 12+12 to 12 Happy Path + 12 Error/Boundary (more coverage for regex classifier edge cases).

---
